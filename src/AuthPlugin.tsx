import React from 'react';

/**
 * Auth Plugin. Registers with the application level. The rendered component will be rendered before any authentication has occurred.
 * @param props The props passed in from the App
 * @returns A rendered dashboard component
 */
export function AuthPlugin(): JSX.Element{
  return <></>;
}

export default AuthPlugin;
